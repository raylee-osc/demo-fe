{
    "compilerOptions": {
        "paths": {
            // * 이미지 파일과 같은 파일들을 포합니다.
            // ! public 디렉토리를 사용하는 것도 가능합니다.
            "@assets": [
                "./src/assets/*"
            ],
            // * 재사용 가능한 컴포넌트 파일들을 포함합니다.
            "@components": [
                "./src/components/*"
            ],
            // * 구동에 필요한 모듈들을 정의한 파일들을 포함합니다.
            "@modules/*": [
                "./src/modules/*"
            ],
            // * 사용자 정의 훅을 정의한 파일들을 포함합니다.
            "@hooks/*": [
                "./src/hooks/*"
            ],
            // * 디자인 정의 파일들이 포합됩니다.
            "@styles/*": [
                "./src/styles/*"
            ],
            // * 기능성 함수들이 작성된 파일들을 포함합니다.
            "@utils/*": [
                "./src/utils/*"
            ],
            // * 프로젝트 소스코드의 루트 폴더경로입니다.
            "@root/*": [
                "./src/*"
            ]
        }
    }
}